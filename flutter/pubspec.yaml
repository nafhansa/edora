name: edora_mobile
description: Minimal Flutter skeleton from FULL_SPEC
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  flutter_blue_plus: ^1.30.0
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  fl_chart: ^0.65.0
  provider: ^6.0.0
  hive: ^2.2.3
  flutter_riverpod: ^2.0.0
  cached_network_image: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
